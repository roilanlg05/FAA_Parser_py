bind 0.0.0.0
port 6379

# Disable frequent RDB forks in this high-throughput dev stack.
save ""
appendonly no

# Keep pubsub clients alive under bursty websocket consumers.
client-output-buffer-limit pubsub 256mb 64mb 120
